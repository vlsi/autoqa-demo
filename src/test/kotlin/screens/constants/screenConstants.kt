package screens.constants

object Timer {
    const val DEFAULT_LOOK_TIME = 20 //default look for elements, Int
    const val TAP_DELAY = 250 // milliseconds, Int
    const val WEBDRIVER_WAIT = 2L // seconds, Long
}